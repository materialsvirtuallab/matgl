name: Linting

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: ["3.10"]

    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        pip install --quiet -r requirements-ci.txt
    - name: mypy
      run: |
        mypy mgnn
    - name: flake8
      run: |
        flake8 --count --show-source --statistics mgnn
        # exit-zero treats all errors as warnings.
        flake8 --count --exit-zero --max-complexity=20 --statistics mgnn
    - name: pylint
      run: |
        pylint mgnn
    - name: pydocstyle
      run: |
        pydocstyle --count mgnn
